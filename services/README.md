# Shared Services

Use this folder for cross-feature integrations and reusable service clients.

- `http/`: fetch wrappers, interceptors, and HTTP client setup.
- `api/`: shared API clients or SDK adapters.
