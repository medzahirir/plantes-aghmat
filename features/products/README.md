# Products Feature

Suggested ownership:

- `domain/`: product entities and rules.
- `application/`: catalog and product use cases.
- `infrastructure/`: product repositories, mappers, and remote/local data sources.
- `presentation/`: product cards, product lists, and feature-local hooks.
