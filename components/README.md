# Shared Components

Use this folder for UI that is reused across multiple features or routes.

- `ui/`: primitive building blocks such as buttons, cards, and inputs.
- `layout/`: app shell pieces such as header, footer, and navigation.
- `shared/`: cross-feature composed components that do not belong to a single feature.
