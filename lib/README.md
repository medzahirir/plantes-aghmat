# Shared Library

Use this folder for framework-agnostic helpers that do not belong to a single feature.

- `config/`: app configuration and environment parsing.
- `constants/`: static values and app-wide constants.
- `utils/`: pure helper functions.
- `validations/`: shared schemas and validation helpers.
